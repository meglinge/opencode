# megcode

Private opencode build distributed as the `megcode` command.

```sh
npm install -g @meglinge/megcode
megcode --version
```

The command uses the same opencode configuration files and directories, so it can coexist with `opencode` while sharing configuration.

Set `MEGCODE_BASELINE=1` before installing on older x64 CPUs that need baseline binaries.
